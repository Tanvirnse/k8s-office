apiVersion: v1
kind: Service
metadata:
  name: {{ .values.appName }}
  labels:
    name: {{ .values.appName }}
    app: {{ .values.appName }}
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  selector:
    name: {{ .values.appName }}
    app: {{ .values.appName }}