apiVersion: v1
kind: Service
metadata:
  name: voting-service
  labels:
    name: voting-service
    app: demo-voting-app
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  selector:
    name: voting-app-pod
    app: demo-voting-app