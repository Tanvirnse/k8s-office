apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .values.name }}
  namespace: {{ .values.namespace }}
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`192.168.3.221.nip.io`)
      priority: 10
      services:
        - name: {{ .values.ingName }}
          port: 80